{{ if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: ldap-tls-master
  labels:
    app: {{ template "openldap.name" . }}-master
    chart: {{ template "openldap.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.extraLabels }}
{{ toYaml .Values.extraLabels | indent 4 }}
{{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.tls.files.master.tlsCrt | b64enc | quote }}
  tls.key: {{ .Values.tls.files.master.tlsKey | b64enc | quote }}
  ca.crt: {{ .Values.tls.files.master.caCrt | b64enc | quote }}
  dhparam.pem: {{ .Values.tls.files.master.dhparamPem | b64enc | quote }}
{{ end }}
